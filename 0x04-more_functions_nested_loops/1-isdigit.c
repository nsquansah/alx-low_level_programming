#include <stdio.h>
#include "main.h"
/**
*
*int _isdigit checks digit 0 to 9
*Return 1 when c is a digit
*Return 0 otherwise
*/

int _isdigit(int c)
{
        if ((c >= 48) && (c <= 57))
        {i
                return (1);
        }
        else
        {
                return (0);
        }

}
